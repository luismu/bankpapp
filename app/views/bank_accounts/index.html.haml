%h1
  Bank Accounts

= link_to new_bank_account_path, class: "btn btn-primary" do 
  New Bank Account

%table.table.table-bordered
  %thead
    %tr
      %th Account Number
      %th Customer
      %th Balance
      %th Actions
  %tbody
    - @bank_accounts.each do |bank_account|
      %tr
        %td #{bank_account.account_number}
        %td 
          = link_to client_path(bank_account.client.id) do
            #{bank_account.client}
        %td #{number_to_currency(bank_account.balance, unit: "COP ")}
        %td
          = link_to bank_account_path(bank_account.id), class: "btn btn-primary" do
            Show